# Webhook Ai MCP

> Webhook management and debugging

`webhook-ai-mcp` is a Model Context Protocol (MCP) server by MEOK AI Labs.

## Install

```bash
pip install webhook-ai-mcp
```

Or run without installing:

```bash
uvx webhook-ai-mcp
```

## Tools

- `validate_webhook_signature` — Validate a webhook signature against a payload and secret. Supports sha256, sha1, md5.
- `log_webhook_event` — Log a webhook event for debugging and replay.
- `replay_events` — Replay/retrieve logged webhook events with optional filters.
- `register_endpoint` — Register a webhook endpoint for monitoring.
- `generate_webhook_secret` — Generate a cryptographically secure webhook signing secret.

## Links

- Source: https://github.com/CSOAI-ORG/webhook-ai-mcp
- PyPI: https://pypi.org/project/webhook-ai-mcp/
- MEOK AI Labs: https://meok.ai
- Council of AI: https://councilof.ai
